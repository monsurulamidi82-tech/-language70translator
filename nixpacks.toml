[phases.setup]
nixPkgs = ["python311", "ffmpeg", "pip"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.start]
cmd = "python bot.py"

[providers.python]
name = "python"
packages = ["python311"]

[environment]
PYTHON_VERSION = "3.11"
